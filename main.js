
function setup() {
    createCanvas(windowWidth, windowHeight);
    fullscreen();
}

function draw() {
    background(200);

    textAlign(CENTER, CENTER);
    textSize(20);
    text("Hello World", width / 2, height / 2);
}